# ai_agent

autonomous ai agents

## Canonical command

Use the HR agent through this command format:

`agent run "<goal>"`

Example:

`agent run "approve pending attendance requests for last week"`

The runtime must treat the quoted text after `run` as the agent goal.
